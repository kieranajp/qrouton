package status

const (
	commandName  = "status"
	commandUsage = "Watch a session's mode, phase, and chords (redraws forever; used by the workspace layout)"

	sessionRootFlag  = "session-root"
	sessionRootUsage = "qrouton session root"
)
