package repos

const (
	commandName  = "repos"
	commandUsage = "Watch a session's repo branches and dirty state (redraws forever; used by the workspace layout)"

	sessionRootFlag  = "session-root"
	sessionRootUsage = "qrouton session root"
)
